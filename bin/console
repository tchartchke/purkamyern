#!/usr/bin/env ruby
# frozen_string_literal: true

require 'bundler/setup'
require 'purkamyern'

# You can add fixtures and/or initialization code here to make experimenting
# with your gem easier. You can also use a different console, if you like.

# (If you use this, don't forget to add pry to your Gemfile!)
# require "pry"
# Pry.start

# newpi1 = Purkamyern::Api.new.get_pokemon(1)
# newpi2 = Purkamyern::Api.new.get_pokemon(2)
# newpi3 = Purkamyern::Api.new.get_pokemon(3)
# newpi4 = Purkamyern::Api.new.get_pokemon(4)

# mydex.scan(3)
# mydex.scan(1)
# mydex.scan(2)
# binding.pry
Purkamyern::Cli.new.call
